<%# Create a search form, add the direction of the search form through url, then use the method %>
<%# 
<%= search_form_for(@query, url: search_path, method: :get, class:'d-flex') do |f| %>
    
    <%# <%= f.search_field :name_or_description_or_artist_or_blockchain_i_cont_any, placeholder: "Search...", class: 'form-search' %>
    
    <%# <%= f.submit "Search", class:'search-button' %>
    <%# <% end %> 