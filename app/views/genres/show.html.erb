<h2><%= @genre.name %></h2>
<ul>
<% @genre.works.each do |work| %>
<li>
<%= link_to work.name, work_path(work.id) %>
<%= cl_image_tag work.picture, alt:work.name, class: 'Genre-NFT' %>
</li>
<% end  %>
</ul>