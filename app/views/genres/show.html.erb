<%# Specific genre name presented at the top of the page %>
<h2><%= @genre.name %></h2>

<%# Loops through all work assoicated to the specific genre individually and extracts specific information regarding each Wrok including Image, name of art, artist and price %>
<div class="show-all">
    <% @genre.works.each do |work| %>
    <%# Similarly created a link to connect them to the work %>
        <%= link_to work_path(work.id) do %>
        <div class="show">
        <%= work.name %>
        <%= cl_image_tag work.picture, alt:work.name, class: 'work-collection' %>
        </div>
        <% end  %>
    <% end  %>
</div>