<h1>Seller Collections</h1>

<h2><%= @user.name %></h2>
<ul>
<% @user.works.each do |work| %>
<li>
<%= link_to work.name, work_path(work.id) %>
<%= cl_image_tag work.picture, alt:work.name, class: 'Sellers-NFT' %>
</li>
<% end  %>
</ul>