<h1 class="user-col">Collections</h1>

<p class="user-p">Find an array of artist and user collections, follow there accounts and see there latest creations. </p>

<div class="all-users">
<% @user.each do |user| %>
    <%= link_to user_path(user.id) do%>
    <div class="user-account">
    <%= cl_image_tag user.profile_picture, alt:user.name, class:'user-pic' %>
    <a><%= link_to user.name %></a>
    </div>
    <br>
    <% end %>
    <% end %>
    
</div>

